# Streamlit Community Cloud uses this file (uv). pyproject.toml is for local editable installs.
# Install the app package plus pinned runtime dependencies:
-e .
streamlit==1.58.0
streamlit-searchbox==0.1.24
pandas==3.0.3
numpy==2.4.6
plotly==6.8.0
pyyaml==6.0.3
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
spacy==3.8.13
defusedxml==0.7.1
tqdm==4.67.1

# spaCy language model (required for word counting; install separately):
#   python -m spacy download de_core_news_lg

# Optional: RSS + Whisper transcription source (heavy, GPU-friendly).
# Install with: pip install -e ".[whisper]"
# torch
# torchaudio
# transformers
# accelerate
# librosa
# soundfile

# Optional: development / tests / security scanning.
# Install with: pip install -e ".[dev]"
# pytest
# pip-audit
